# Server

## Quick start

```bash
npm install
npm start
```
> **Warning**  
> Don't forget to set the configuration of the SQL database (user and password) in: server/app/config/db.config.js

## Adding administrators
```
npm start
```
Then in a web browser, open http://localhost:8080/admin-panel.

Fill in all the information and validate the form.
